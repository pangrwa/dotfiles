-- this file is to setup plugins that don't require a lot of configuration
-- names point to the github repo (lazy handles)
return {
  "nvim-lua/plenary.nvim", -- lua functions that many plugins use
  "christoomey/vim-tmux-navigator", -- tmux & split window navigation
}

